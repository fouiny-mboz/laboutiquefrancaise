{% extends 'base.html.twig' %}

{% block title %}Compte - La Boutique Francaise!{% endblock %}

{% block content %}
<a href="{{ path('account_address_add') }}" class="btn btn-info float-right">Ajouter une adresse</a>
<h1>Mes adresses</h1>
<p>C'est dans cet espace personnel que vous allez gerer toutes vos adresses.</p>
<a href="{{ path('account') }}">Retour</a>
<hr>
{% if app.user.adresses|length == 0 %}
<p class="text-center">
    Vous n'avez pas encore ajouter d'adresses dans votre compte client. Pour en ajouter une, veillez <a hrf=""> clicquez ici</a>
</p>
{% else %}
<div class="row">
{% for address in app.user.adresses %}
<div class="col-md-4">
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{ address.name }}</h5>
            <p class="card-text">
                {{ address.address }}<br />
                {{ address.postal }} -  {{ address.city }} - {{ address.country }}
            </p>
            <a href="{{ path('account_address_edit', {'id': address.id} ) }}">Modifier</a>|<a href="{{ path('account_address_delete', {'id': address.id} ) }}">Suprimer</a>
        </div>
    </div>
</div>
{% endfor %}
</div><br/>
{% endif %}
{% endblock %}
